# Dispatch — <run id>

_Generated by conductor on <YYYY-MM-DD HH:MM UTC>._

## Run summary

<one paragraph: what this run is producing, why now>

## Run id

<.workforce/runs/<ISO_TS>>

## Iteration

1 of 3 _(convergence iteration counter; max 3 per intent)_

## Wave order

1. <wave 1 task names — these run in parallel terminals>
2. <wave 2 task names — start after wave 1 reports DONE>
3. <wave 3 task names>

## Tasks

### Task 1 — <engineer-role>

- **Territory**: <file globs the engineer may write to>
- **Inputs**: <list of artifacts this task reads>
- **Outputs**: <list of artifacts this task writes>
- **Verification**: <literal command(s) the engineer runs to prove DONE — e.g. `python3 -c "import json; json.load(open('architecture.md'))"`, `test -f architecture.md && grep -q '## Components' architecture.md`>
- **Success criteria**:
  - <bullet 1; falsifiable>
  - <bullet 2; falsifiable>
- **Escalation**: <when to write BLOCKED.md — e.g. "if security-model.md is missing or has fewer than 3 Phase 0 items">
- **Permission mode**: `default` | `plan` | `auto` | `bypass` _(optional; defaults to `default`)_
  - `default` — engineer asks before any tool call outside declared territory.
  - `plan` — engineer must NOT execute; produces plan-level artifacts only.
  - `auto` — engineer is empowered to execute within the envelope without asking.
  - `bypass` — engineer may act outside the envelope's territory; document each excursion.

### Task 2 — <engineer-role>

- **Territory**: ...
- **Inputs**: ...
- **Outputs**: ...
- **Verification**: ...
- **Success criteria**: ...
- **Escalation**: ...

<repeat for each task in this dispatch>

## Spawn commands

```
# Wave 1 (run each in a separate terminal):
python3 scripts/pipeline_runner.py spawn-payload --payload <task-1-payload.json>
python3 scripts/pipeline_runner.py spawn-payload --payload <task-2-payload.json>

# After wave 1 reports DONE (run conductor --mode=monitor to check):
python3 scripts/pipeline_runner.py spawn-payload --payload <wave-2-task-1.json>
```
